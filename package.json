{
  "name": "better-sol-monorepo",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ],
    "catalog": {
      "typescript": "^6",
      "zod": "^4.4.3",
      "oxlint": "^1.62.0",
      "@types/bun": "^1.3.13",
      "@t3-oss/env-core": "^0.13.11"
    }
  },
  "scripts": {
    "clean": "bun run --workspaces clean",
    "check": "bun run --workspaces check",
    "build": "bun run --workspaces build",
    "test": "bun run --workspaces test",
    "lint": "bun run --workspaces lint",
    "lint:fix": "bun run --workspaces lint:fix",
    "format": "bun run --workspaces format",
    "format:check": "bun run --workspaces format:check",
    "compiler:dev": "bun run --filter compiler-api dev",
    "compiler:start": "bun run --filter compiler-api start",
    "compiler:check": "bun run --filter compiler-api check"
  }
}